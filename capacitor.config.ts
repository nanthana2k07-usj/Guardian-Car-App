import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.guardiancar.app',
  appName: 'GuardianCar',
  webDir: '.output/public',
};

export default config;
